  
for the frontend, node_modules is ignored so it doesn't be uploded to github repo because of its huge file size.

so to run the frontend, you have to install npm using this line to be inserted in terminal:

1- cd frontend

2- npm install --legacy-peer-deps
